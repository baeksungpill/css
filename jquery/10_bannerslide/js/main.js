$(function(){
 $('.list').slick({
    // 자동실행옵션
    autoplay: true,
    // 한화면에 보일 갯수
    slidesToShow:2,
    // next버튼 2개씩 가게 하는거
    slidesToScroll:1,
    // 좌우버튼 숨기기
    arrows:false,
    // 페이지 버튼 보이기
    dots:true
});



})